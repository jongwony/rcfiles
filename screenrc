shell $SHELL

autodetach on
startup_message off
hardstatus on
hardstatus alwayslastline
hardstatus string "%{WK}%-w%{KW}%t[%n]%{-}%+w %=%{G} %H %{..Y} %Y-%m-%d"

bind ^C stuff "screen -l^M"
bind c stuff "screen -l^M"
bind ^S split
bind s split
bind ^X remove
bind x remove
